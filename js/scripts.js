$(document).ready.(function() {
  $("#cards").click(function() {
 $("#showCards").show();
 $("#nocards").hide();
 });
 var lefts = ["2","3","4", "5", "6", "7", "8", "9", "10", "Jack", "Queen","King", "Ace"];
 var rights = ["hearts", "spades", "diamonds", "clubs"];

 var "showCards" = $(lefts.forEach(function(left) {
   alert(left + " of " +right);
 });

//  var lefts = ["2","3","4", "5", "6", "7", "8", "9", "10", "Jack", "Queen","King", "Ace"];
//  var rights = ["hearts", "spades", "diamonds", "clubs"];

///  lefts.forEach(function(left) {
    //alert(left + " of " +right);
//  var left = lefts[Math.floor(Math.random() * lefts.length)];
  //var right = rights[Math.floor(Math.random() * rights.length)];
  });
});
